export enum SimulationStatus {
    NOT_STARTED,
    RUNNING,
    STOPPED,
    COMPLETE
}

export type Security = {
    symbol: string,
    name: string,
}

export type AlpacaSecurity = {
    symbol: string,
    name: string,
}
